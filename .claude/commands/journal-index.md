# Journal Index Content

Index markdown files from directory: $ARGUMENTS

```bash
agent-journal content index $ARGUMENTS
```

Options:
- `--project <name>` - Associate with project
- `--filter "*.md"` - Glob pattern (default: *.md)
- `--rebuild` - Clear and rebuild index
